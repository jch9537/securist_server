//TODO - req, res빼기 rest 없애기
const { Auth, Repository, SendMail } = require('../outbound');
const {
    SignUp,
    CreateUser,
    GetUser,
    UserEntity,
    DeleteUserByAdmin,
} = require('../../domain/user');

module.exports = {};
